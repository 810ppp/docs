---
title: フィルタ優先度
description: 複数フォルダにマッチする場合の振り分けルール
---

# フィルタ優先度

複数フォルダにマッチする場合、基本的に次の優先度で振り分けられます。

1. より深い階層のフォルダ（子 > 親）
2. 同階層では、名前系 > 拡張子 > 種別（音声/動画/画像など）

<Example>
  `01_Image`（画像フィルター）と `PSD`（拡張子 `.psd` フィルター）がある場合、PSDファイルは `PSD` フォルダに振り分けられます。
</Example>
